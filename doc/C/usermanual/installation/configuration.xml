<para>
&app;, like most GNU projects, allows a user to select various options before compiling its source code. These options include selecting from the available features, specifying custom paths for installation, and cross compiling.  &app; uses <ulink type="http"
  url="http://www.gnu.org/software/autoconf/">GNU Autoconf</ulink>
  for configuration.
</para>
<para>
  If you opted to download the 
  <link linkend="sourcerepo">development checkout</link>
  of &app;, the <emphasis>configure</emphasis> script will
  not be included.  It can be created by running 
  <emphasis>autogen.sh</emphasis> from the source root directory:
  <programlisting>
  ./autogen.sh
  </programlisting>
  Note that there are some 
  <link linkend="codedepend">dependencies</link> for
  autogen.
</para>
<para>
  All the standard <emphasis>configure</emphasis> options
  are avaiable.  In addition, &app; has two types of
  options: those which <link linkend="features">enable or disable 
  features</link>, and
  those which <link linkend="custompath">specify custom paths for 
  development packages</link>
  which aren't found during the default search.  A complete
  list of <emphasis>all</emphasis> configuration options, including
  standard ones, can be seen by typing:
  <programlisting>
  ./configure --help
  </programlisting>
  Read further for a more detailed explanation of &app;-specific
  options.
</para>
<para>
  The syntax for running <emphasis>configure</emphasis> is as follows:
  <programlisting>
  configure &lt;options&gt;
  </programlisting>
  The example below shows the <emphasis>configure</emphasis> options
  which create the smallest working standalone version of &app;.  In
  this example, <emphasis>configure</emphasis> is being run from the
  source root directory:
</para>

<screen>
<userinput>
./configure --disable-debugger --disable-cygnal --disable-docbook \
--disable-plugin --enable-media=mad --enable-gui=sdl
</userinput>
</screen>

<sect2 id="features">
  &featureoptions;
</sect2>
         
<sect2 id="custompath">
  &custompath;
</sect2>

<sect2 id="crosscompilingconfiguration">
  &cross_compiling;
</sect2>
