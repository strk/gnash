application/x-shockwave-flash:swf:Shockwave Gnash
	nokill stream embed noisy ignore_errors hidden fill swallow(Gnash) loop: gnash -v -j $width -k $height "$file" -x $window
	stream: gnash -j $width -k $height -v "$file" -x $window
